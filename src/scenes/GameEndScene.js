import { Scene } from 'phaser';
import Constant from '../constant/gameConstant';


export default class gameEndScene extends Scene {
  constructor() {
    super({
      key: Constant.SCENENAME.gameEnd
    });
  }

  init() {
    
  }

  preload() {
    
  }

  create() {
    
  }
}
